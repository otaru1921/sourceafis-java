
public class Backups {
	// Parte do Carregador de Arquivo
			//Table janela = new Table("3");
			//janela.run();
			//janela.select("1");
			
			
			// Parte de Comparação de Imagem
			/*byte[] probeImage = Files.readAllBytes(Paths.get("G:\\FingerPrints\\nivel1.tif"));
			byte[] candidateImage = Files.readAllBytes(Paths.get("G:\\FingerPrints\\match1.tif"));

			FingerprintTemplate probe = new FingerprintTemplate(
			    new FingerprintImage()
			        .dpi(500)
			        .decode(probeImage));

			FingerprintTemplate candidate = new FingerprintTemplate(
			    new FingerprintImage()
			        .dpi(500)
			        .decode(candidateImage));
			
			double score = new FingerprintMatcher()
				    .index(probe)
				    .match(candidate);
			
			double threshold = 40;
			boolean matches = score >= threshold;
			System.out.println(score);
			System.out.println(matches);
			*/
			// Fim da Comparação
}
